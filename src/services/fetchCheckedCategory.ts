// import axios from "axios";
// import config from "../../config/config";

// const fetchCheckedCategory = async (categoryId: any) => {
//   try {
//     const response = await axios.get(`${config.api}/category/${categoryId}`);
//     return response.data;
//   } catch (error) {
//     throw new Error("Error fetching in checked category: " + error);
//   }
// };
// export default fetchCheckedCategory;
